import datetime

def interfaceCredenciaç():
        id = input()
        senha = input()

        

def datasComecoFinal():
    dataComeco = input("Data de comeco (yyyy-mm-dd)")
    dataFinal = input("Data final (yyyy-mm-dd, dia nao incluido)")
    return [dataComeco, dataFinal]